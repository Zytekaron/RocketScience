package main

import (
	"bufio"
	"bytes"
	"context"
	crand "crypto/rand"
	"encoding/base32"
	"encoding/base64"
	"encoding/binary"
	"encoding/csv"
	"encoding/json"
	"encoding/xml"
	"fmt"
	"io"
	"io/ioutil"
	"math"
	"math/big"
	"math/cmplx"
	"math/rand"
	"net"
	"net/http"
	"net/url"
	"os"
	"strconv"
	"strings"
	"time"
)

func main() {
	{{code}}
}

func you_cant_see_me() string {
	_ = []interface{}{
		bufio.NewReader,
		bytes.NewReader,
		context.Background,
		crand.Read,
		base32.NewDecoder,
		base64.NewDecoder,
		binary.Read,
		csv.NewReader,
		json.NewEncoder,
		xml.NewDecoder,
		fmt.Println,
		io.WriteString,
		ioutil.ReadAll,
		math.Round,
		big.NewInt,
		cmplx.Abs,
		rand.New,
		net.Dial,
		http.Post,
		url.Parse,
		os.Create,
		strconv.Itoa,
		strings.ToLower,
		time.Now,
	}
	return "what the fuck are you doing"
}
